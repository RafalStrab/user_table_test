{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ClientAddress creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('clientaddress') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
